#!/bin/bash
# Author: Eliza
# Date: 05/21/2021

hostname  # will print hostname 
uptime #  command shows how long the system has been running. It also displays the current time, the number of logged-in users, and the system load averages for the past 1, 5, and 15 minutes
df -hTP # will print disk free in h–human-readable,print sizes in power of 1024; T–print-type : print file system type; P–portability,use POSIX output format  use POSIX output format,
free -h # this command will print memory in human readable format G, M, B